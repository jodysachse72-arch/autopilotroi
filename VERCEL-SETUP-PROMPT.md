# Vercel Setup Prompt — Retired

This one-time Antigravity setup prompt is no longer part of the operating workflow.

The AutoPilotROI GitHub repository is connected to the AutoPilotROI Vercel project, branch deployments are available, and the shared ChatGPT project is now the collaboration surface. Vercel Comments are optional.

Do not rerun the historical setup exercise or create a throwaway certification branch. For current work, follow `WORKFLOW.md`. Certify Barry's workflow with a small real change he actually wants.
